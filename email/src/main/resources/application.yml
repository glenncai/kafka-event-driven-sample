server:
  port: 8001
  servlet:
    context-path: /api
spring:
  application:
    name: @application.name@
logging:
  level:
    root: INFO
    com.glenncai.kafkaeventdrivensample.email: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE